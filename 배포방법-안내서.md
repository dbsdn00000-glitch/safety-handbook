# 현장안전 핸드북 — 모바일 배포(PWA) 안내서

이 폴더에 있는 파일들을 인터넷에 올려두면, 근로자들이 스마트폰 브라우저로
주소를 열고 "홈 화면에 추가"만 하면 진짜 앱처럼 아이콘이 생기고
전체화면으로 실행됩니다. (앱스토어 등록 필요 없음, 비용 없음)

폴더 안 파일 구성
```
pwa-package/
 ├─ index.html          ← 앱 본체
 ├─ manifest.json        ← 앱 이름/아이콘/색상 설정
 ├─ sw.js                 ← 오프라인에서도 열리게 해주는 파일
 ├─ icons/                ← 홈 화면에 표시될 아이콘
 └─ images/               ← 홈 화면 배경사진
```
이 구조를 그대로 유지한 채로 통째로 업로드해야 합니다. (파일 하나만
따로 옮기면 안 됨)

---

## 방법 A. GitHub Pages 사용 (무료, 가장 간단, 추천)

### 1단계. GitHub 계정 만들기
1. https://github.com 접속 → 우측 상단 **Sign up**
2. 이메일, 비밀번호, 아이디 입력 후 가입 (무료)

### 2단계. 새 저장소(repository) 만들기
1. 로그인 후 우측 상단 **+** 버튼 → **New repository** 클릭
2. Repository name에 영문으로 이름 입력 (예: `safety-handbook`)
3. **Public** 선택 (Pages 무료 사용을 위해 공개로 설정)
4. **Create repository** 클릭

### 3단계. 파일 업로드
1. 방금 만든 저장소 페이지에서 **Add file → Upload files** 클릭
2. 이 `pwa-package` 폴더 안에 있는 파일과 폴더를
   **전부 선택해서** 화면에 드래그 앤 드롭
   (index.html, manifest.json, sw.js, icons 폴더, images 폴더 — 모두 함께)
3. 하단 **Commit changes** 클릭 → 업로드 완료

### 4단계. GitHub Pages 켜기
1. 저장소 상단 메뉴에서 **Settings** 클릭
2. 왼쪽 메뉴에서 **Pages** 클릭
3. **Branch** 항목에서 `main` 선택, 폴더는 `/ (root)` 선택 → **Save**
4. 1~2분 기다리면 페이지 상단에
   `Your site is live at https://아이디.github.io/safety-handbook/`
   같은 초록색 안내가 뜹니다. 이 주소가 배포된 앱 주소입니다.

### 5단계. 스마트폰에서 열기 + 홈 화면에 추가

**Android (크롬 브라우저)**
1. 위에서 받은 주소를 크롬에서 접속
2. 우측 상단 점 3개(⋮) 메뉴 → **홈 화면에 추가** 또는 **앱 설치** 선택
3. 이름 확인 후 **추가** → 홈 화면에 아이콘 생성됨

**iPhone (Safari 브라우저 — 꼭 Safari로 열어야 함)**
1. 주소를 Safari에서 접속
2. 하단 공유 버튼(네모+화살표 ⬆️) 탭
3. 아래로 스크롤해서 **홈 화면에 추가** 선택
4. 이름 확인 후 **추가** → 홈 화면에 아이콘 생성됨

이후 아이콘을 누르면 브라우저 주소창 없이 전체화면 앱처럼 실행됩니다.

---

## 방법 B. 사내(회사) 웹서버가 이미 있는 경우
IT 담당 부서에 `pwa-package` 폴더 전체를 그대로 전달해서
사내 웹서버(인트라넷)에 올려달라고 요청하면 됩니다.
파일 구조와 이름을 바꾸지 않아야 합니다. 이후 스마트폰에서 열고
"홈 화면에 추가" 하는 방법은 위 5단계와 동일합니다.

---

## 나중에 내용을 수정하고 싶을 때
1. `index.html` 파일을 텍스트 에디터로 열어 `CATEGORIES` 데이터 부분 수정
   (자세한 방법은 이전에 안내드린 내용과 동일합니다)
2. `sw.js` 파일 맨 위 `CACHE_NAME = 'safety-handbook-v1'` 의 숫자를
   `v2`, `v3` 처럼 올려주세요. (이 숫자를 안 올리면 스마트폰이
   예전 버전을 계속 캐시해서 보여줄 수 있습니다)
3. GitHub 저장소에 다시 **Add file → Upload files** 로 바뀐 파일을
   올리면 자동으로 반영됩니다. (Commit changes 클릭)

## 참고
- 사진을 images 폴더에서 교체할 경우, 파일명을 그대로 `그림1.jpg`로
  유지하거나, 바꾼 파일명을 `index.html`의 `HOME_BG_IMAGE` 부분과
  `manifest.json`, `sw.js`의 경로에도 동일하게 맞춰주세요.
- 아이콘(icons 폴더)도 같은 방식으로 다른 이미지로 교체 가능합니다.
  (192x192, 512x512 크기의 정사각형 PNG 권장)
